name: 'Antigravity PR Review'
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: リポジトリのチェックアウト
        uses: actions/checkout@v4

      - name: Antigravity CLI のインストール
        run: curl -fsSL https://antigravity.google/cli/install.sh | bash

      - name: コードレビューの実行
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          TERM: xterm-256color
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          script -e -c "~/.local/bin/agy --dangerously-skip-permissions --log-file=/dev/stderr review --pr \"${{ github.event.pull_request.number }}\"" /dev/null
